export interface Template {
  id: string;
  name: string;
  category: string;
  description: string;
  content: string;
}

export interface TemplateMetadata {
  id: string;
  name: string;
  category: string;
}
